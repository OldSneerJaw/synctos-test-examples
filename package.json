{
  "name": "synctos-test-examples",
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^4.1.0",
    "synctos": "^2.0.0"
  },
  "scripts": {
    "build": "synctos src/my-example-doc-definitions.js build/my-example-sync-function.js",
    "test": "npm run validate && npm run build && mocha",
    "validate": "synctos-validate src/my-example-doc-definitions.js"
  }
}
