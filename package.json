{
  "name": "synctos-test-examples",
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^10.1.0",
    "synctos": "^2.7.1"
  },
  "scripts": {
    "build": "npm run validate && synctos src/my-example-doc-definitions.js build/my-example-sync-function.js",
    "clean": "rm -rf build",
    "test": "npm run validate && mocha",
    "validate": "synctos-validate src/my-example-doc-definitions.js"
  }
}
